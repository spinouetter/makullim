# 요청 #0051 — 막공 규칙 개편: 개별 조합 페어막 + 점선 표시

상태: 완료 <!-- 대기 / 진행 중 / 완료 / 보류 -->

---

## 요청 (작성: Spinouetter)

<!-- 대화로 전달된 요청을 Claude가 옮겨 적음 -->

- 배역별 막공은 마지막 공연이 미정이면 표시하지 않는다. 페어막은 이 규칙을 무시하고 표시.
- 한 배역에 배우가 둘 이상 참가한 회차는 각각의 조합에 대해, 한 조합이라도 막공일 때만 표시:
  - 막공 조합이 하나면 — 막공인 배우들을 가로 정렬시켜 점선으로 모은다.
    예: 빌리 [김우진, 박지후] / 마이클 [이루리]에서 (김우진, 이루리)가 막공이면
    표에는 [김우진, 박지후] / [이루리, 빈칸]으로 만들고 김우진·이루리만 묶는다.
  - 둘 이상 섞여 있으면 — 그냥 전체를 점선으로.
- (후속 정정) 배우 표기는 **출연 순서라 정렬 금지**. [A,B]/[C,D]에서 (A,D)·(B,C)가
  막공이면 이 둘을 **각각 따로** 묶어 달라. 배역별 막공은 "마지막 공연이 미정이면
  표시 안 함"으로.

---

## 정리 (작성: Claude)

- **배역별 막공** — 스케줄 **마지막 회차**의 그 배역 캐스팅이 미정이면 진짜 막공을 알 수
  없으므로 **그 배역 전체를 표시하지 않음**. 마지막 회차가 발표된 배역은 전 배우 표시.
  (기존 '발표 경계' 회차 단위 제외 규칙을 이 배역 단위 규칙으로 교체)
- **페어막** — 위 규칙 미적용(발표 범위 안에서 항상 표시). 조합의 단위를 기존
  '배역별 배우 집합'에서 **배역당 1명씩 뽑은 개별 조합**(카티전 곱)으로 변경, 개별 조합의
  마지막 동반 출연 회차를 계산.
  - 전 배역 1명씩인 회차: 기존 **실선 타원**(full).
  - 다중 배우 회차 & **모든** 조합이 막공: **셀 전체 점선**(all).
  - 다중 배우 회차 & 일부 조합만 막공: **조합별로** 배우마다 점선 상자를 치고 같은
    조합끼리 점선 연결선으로 잇는다(combos). 출연 순서는 그대로 유지되며, (A,D)·(B,C)
    같은 교차 조합도 각각 따로 묶인다. 상자·연결선은 렌더 후 실제 좌표를 재서 SVG
    오버레이(`drawPairComboOverlay`)로 그리고, 창 크기 변경 시 다시 그린다.

## 할 일 (작성: Claude)

- [x] app.js — `buildLastShowMap` 배역 단위 규칙으로 교체(`isFinalDayPerf` 제거)
- [x] app.js — `buildPairLastSet` → `buildPairLastInfo`(개별 조합·모드 판정: full/all/combos)
- [x] app.js — renderSchedule: 모드별 셀 클래스(pairlast/pairdot/paircombo),
      combos 모드 이름 표식(`pair-mate`+`data-pairc`) — 출연 순서 유지
- [x] app.js — `drawPairComboOverlay`(SVG 상자·연결선, 렌더 후·창 크기 변경 시 재그림)
- [x] styles.css — 점선 전체(`pairdot`)·pair-mate 여백·여유 패딩 셀렉터 paircombo
- [x] 캐시버스터 — app.js v120 · styles v87
- [x] 검증 — 빌리 2026 실데이터(전부 full 모드가 정답임을 계산으로 확인) + 임시 조작
      데이터로 line/all 모드 렌더 확인(검증 후 원복), 배역별 규칙은 마지막 회차 발표
      배역(성인빌리·할머니 등)만 pill 표시되는 것 확인

## 결정 사항 (작성: Claude)

- **개별 조합 의미 변경** — 기존에는 '그날의 배우 구성 전체'가 같아야 같은 조합이었지만,
  이제 (배역당 1명) 개별 조합의 **동반 출연** 기준. 예: 빌리 2명 회차는 (빌리A×마이클),
  (빌리B×마이클) 두 조합이 각각 그날 출연한 것으로 본다.
- **(폐기) line 모드** — 처음엔 막공 배우를 윗줄로 정렬해 한 줄 점선으로 묶었으나,
  배우 표기가 출연 순서라 정렬 불가 + 교차 조합 요구로 SVG 오버레이(combos)로 대체.
- **combos 오버레이** — 이름 span에 `pair-mate`+`data-pairc`(속한 조합 번호들)만 남기고,
  상자(둥근 점선 rect)와 연결선(인접 배역 상자끼리 dashed line)은 표 스크롤 컨테이너
  안의 절대배치 SVG가 그린다(가로 스크롤과 함께 이동). 한 배우가 여러 막공 조합에
  속하면 상자 하나에 연결선이 여러 개 뻗는다.
- 배역이 미정인 회차는 페어 조합 자체가 성립하지 않으므로 기존대로 제외.
