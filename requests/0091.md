# 요청 #0091 — 재관카드 이미지 도장(손글씨) + 회차(시간) 단위 도장 규칙

상태: 완료

---

## 요청 (작성: Spinouetter)

<!-- 대화로 요청됨 -->

- (손글씨 이미지: "사랑해 영원히 -우진 빌리-") 재관 카드 도장. 오늘(7/25) 낮공연 이걸로 바꾸어줘.

---

## 정리 (작성: Claude)

재관카드(도장판, 0087)의 도장은 지금까지 텍스트 라벨(`EXCELLENT`/`HOME COMING`)을 CSS로 꾸민 자국뿐이었다. 이번 요청은 특정 회차(2026-07-25 낮공연)의 도장을 **손글씨 이미지 도장**으로 바꾸는 것. 두 가지가 필요했다.

1. **이미지 도장 지원** — `stamp.json`의 `stampTypes[]` 항목에 `image`(공연 폴더 상대 경로)를 주면, 도장 자국·타입 선택 버튼이 텍스트 대신 그 이미지를 그린다(데이터 주도 — 코드에 특정 도장 하드코딩 없음).
2. **회차 단위 규칙** — 기존 `rules[]`는 날짜 범위(from/to)만 지원해 같은 날 낮/밤을 구분할 수 없었다. 규칙에 선택 필드 `time`("HH:MM")을 추가해, 있으면 그 시간 회차에만 적용되게 했다.

## 할 일 (작성: Claude)

- [x] 손글씨 PNG를 투명 배경(잉크 진하기→알파)으로 변환·트림·축소(520px)해 `shows/betm-skorea-2026/images/stamp-woojin-billy.png`로 저장.
- [x] `js/stamp.js`: `stampType(id)`·`stampMarkHtml(id)` 추가 — `image` 있는 타입은 `<img>` 도장, 없으면 기존 텍스트 자국. 셀 렌더(413행 부근)와 타입 선택 버튼(`stampTypeButtons`)에 적용.
- [x] `js/stamp.js`: `stampTypeForDate(dateStr, timeStr)` — 규칙에 `time`이 있으면 회차 시간까지 일치해야 적용. 호출부 3곳(`autoStamp`·픽 목록 `hc` 표시·픽 선택)에 `w.time` 전달.
- [x] `js/stamp.js`: `CFG_VER` 10 → 11 (stamp.json 내용 변경).
- [x] `css/styles.css`: `.stamp-mark.img`(테두리·패딩 없음, -6° 기울임, 최대 76×36px), `.stamp-type-btn.has-img` 이미지 버튼 스타일.
- [x] `shows/betm-skorea-2026/stamp.json`: `stampTypes`에 `woojin-billy`(label "사랑해 영원히 -우진 빌리-", image) 추가, `rules`에 `{from/to: 2026-07-25, time: "14:00"}` 추가.
- [x] `index.html`: `styles.css?v=176→177`, `stamp.js?v=35→36`.
- [x] headless Chromium 미리보기로 렌더·콘솔 에러 확인.

## 결정 사항 (작성: Claude)

- **이미 찍힌 도장은 소급되지 않음** — 슬롯은 찍는 시점의 타입을 저장하므로, 7/25 낮공연을 이미 `EXCELLENT`로 찍어 뒀다면 그 칸을 지우고 다시 찍어야(자동/픽) 새 도장이 적용된다. 앞으로 찍는 것은 규칙대로 자동 적용.
- 규칙 `time`은 선택 필드 — 기존 규칙(날짜 범위만)은 동작 불변.
- 이미지 캐시는 기존 `imgVer` 체계를 그대로 사용(새 파일이라 버전 인상 불필요).
- 도장 이미지는 잉크(어두운 픽셀)만 남긴 투명 PNG라 다이어리 배경 위에 실제 도장처럼 얹힌다. 색은 원본 검정 유지.

### 후속 (같은 날 대화 요청)

- 남은 3회차에도 "이름 빌리" 도장을 준비(지우고 다시 찍으면 적용): `stampTypes`에 `seungju-billy`(승주 빌리)·`yunwoo-billy`(윤우 빌리)·`jihoo-billy`(지후 빌리) — 아직 손글씨 이미지가 없어 **텍스트 라벨 placeholder**, 이미지가 오면 `image`만 추가하면 됨. `rules`에 7/25 19:00·7/26 14:00·7/26 19:00 회차 규칙 추가. `CFG_VER` 12, stamp.js v37.
- `.stamp-mark` 기본색을 `#a43233`로 지정(styles.css v178) — 개별 타입 CSS 없는 새 도장이 테마 잉크색을 상속해 흰 다이어리 위에서 안 보이던 문제. 공연별 타입 id를 공용 CSS에 하드코딩하지 않기 위한 데이터 주도적 해법.
