# 요청 #0087 — 재관카드(Stamp) 기능: 빌리 Billy's Diary 도장판

상태: 완료

---

## 요청 (작성: Spinouetter)

<!-- 대화로 요청됨 -->

Stamp 기능을 만들거야. 개발하는 동안에는 앵커 또는 쿼리 `stamp`로 접근 가능.

- 빌리 엘리어트는 공연을 볼 때마다 스탬프를 찍어주고, 스탬프가 일정 갯수 이상이 되면 선물을 준다. 도장판은 **10칸**, 선물은 **3·7·10회** 때 각각 체크박스가 있다.
- 이 페이지에는 **닫힌 도장판과 펼쳐진 도장판**이 나열된다. 10개 다 찍힌 건 닫힘(표지), 다 안 찍힌 건 펼침.
- **자동** 버튼: 지금까지 내 공연 중 도장 안 찍은 공연을 찾아 순서대로 도장을 찍고, 10개가 다 차면 새 도장판을 만든다.
- 도장판 이름은 **Billy's Diary**, 새 도장판은 **Billy's Diary #n**(나중에 바꿀 수 있음).
- 도장판은 닫고 열 수 있다. 도장판에는 스탬프 칸과 날짜 칸이 있다. 스탬프는 **EXCELLENT / HOME COMING** 두 개. 날짜는 `7/14`, 같은 날 공연이 둘이면 `7/14-1`, `7/14-2`.
- **HOME COMING** 스탬프는 6/16–6/21 공연. **5/12–5/17**은 도장 두 개 찍어주는 날(하나가 마지막 칸이면 하나 더 만들어서 찍기).
- 열기 후 도장/날짜를 누르면 **지울지·바꿀지** 물어보는 선택지가 있다.
- **다른 도장 찍기**로 임의의 날짜로 찍을 수 있고, 이 때는 **메모 삽입** 가능.
- 도장판별 **비우기**, **전체 지우기** 버튼과 **도장판 삭제**도 만들 것.
- 기능(페이지) 이름은 **재관카드**. 표지·도장판은 **제공한 이미지를 그대로** 사용.

---

## 정리 (작성: Claude)

**재관카드(Stamp)** 는 공연을 볼 때마다 도장을 찍는 다이어리형 트래커다. `finale.js`처럼 **app.js 이후 로드되는 독립 모듈 `js/stamp.js`(IIFE)** 로 구현했고, 설정은 **공연 데이터(`shows/<id>/stamp.json`)** 에서 읽어(데이터 주도) 코드에는 빌리 전용 값이 없다. 사용자 상태(찍힌 도장·이름·펼침·선물체크)는 자체 localStorage 키(`makollim:stamp:v1:<id>`)에 저장한다.

- **표지/도장판은 제공 이미지 그대로** — 표지(`diary-cover.jpg`)·도장판(`diary-board.jpg`)을 `shows/<id>/images/`에 축소 저장하고, 도장판 이미지 **위에 도장·날짜·선물체크를 절대좌표(이미지 비율)로 오버레이**한다. 좌표(그리드·선물 □ 위치)는 `stamp.json`에 두어 이미지가 바뀌면 값만 조정하면 된다.
- **닫힘/펼침** — 10칸이 다 차면 자동으로 닫혀 표지로 보이고, 표지 클릭 또는 `열기`로 펼친다. 펼치면 도장판 표가 나온다.
- **자동** — `performanceData.performances` 중 `isEnded && hasSeat`(관극)인 회차를 시간순으로 훑어, 아직 안 찍은 회차(sid 기준)를 순서대로 찍는다. 마지막 도장판이 차면 새 도장판을 만든다.
- **도장 종류·개수·날짜** — 규칙은 `stamp.json`. HOME COMING은 날짜 범위(6/16–6/21) 규칙, 그 외 EXCELLENT. 더블데이(5/12–5/17)는 한 회차에 2개(칸이 넘치면 다음 도장판으로 이어짐). 날짜는 `M/D`, 같은 날 관극이 둘 이상이면 `-1/-2` 접미사.
- **편집** — 펼친 도장판에서 도장/날짜 칸을 누르면 팝오버로 **바꾸기(도장종류·날짜·메모)·지우기**. 빈 칸을 누르면 그 칸에 직접 찍기.
- **다른 도장 찍기** — 임의 날짜 + 메모로 다음 빈 칸에 찍기.
- **비우기/전체 지우기/삭제** — 도장판별 비우기(이름 유지), 전체 지우기(초기화), 도장판 삭제.
- **개발용 게이트** — 상시 탭 없이 `#stamp` 또는 `?stamp`로만 접근(정식 승격 전). app.js는 손대지 않고 stamp.js가 페이지를 직접 활성화한다.

## 할 일 (작성: Claude)
- [x] `js/stamp.js` 신규 — 모듈(IIFE). 설정 로드(`stamp.json`)·상태 저장(자체 localStorage)·렌더(표지/펼침 오버레이)·자동/편집/다른도장/비우기/전체지우기/삭제·`#stamp|?stamp` 게이트.
- [x] `shows/betm-skorea-2026/stamp.json` 신규 — title·slots·이미지/비율·그리드 좌표·선물행/좌표·스탬프종류·기본스탬프·HOME COMING 규칙·더블데이.
- [x] `shows/betm-skorea-2026/images/diary-cover.jpg`·`diary-board.jpg` — 제공 이미지 축소본.
- [x] `index.html` — `#page-stamp` 섹션(툴바: 자동/다른 도장 찍기/새 도장판/전체 지우기)·`js/stamp.js` 로드·`styles.css` v148→v149. (상시 탭 버튼은 아직 없음)
- [x] `css/styles.css` — 재관카드 스타일(툴바·이미지박스/오버레이·도장자국·선물✓·팝오버·토스트).
- [x] 미리보기(headless Chromium)로 정렬·자동·편집·메모·영속성·무회귀 확인.
- [x] 재관카드 **순서 바꾸기(드래그 정렬)** — 커버 길게 누르면 그 카드를 집어 바로 드래그(별도 정렬 모드 없음), 나머지는 회색+50%로 de-highlight, 위/아래로 놓으면 즉시 정렬 완료, 가장자리 근처면 자동 스크롤. stamp.js ?v=26, styles.css v167.

## 결정 사항 (작성: Claude)
- **데이터 주도** — 빌리 전용 값(도장 규칙·이미지·좌표·선물 위치)은 전부 `stamp.json`. 다른 공연은 자기 `stamp.json`을 두면 코드 수정 없이 동작(없으면 기본값으로 빈 도장판만).
- **app.js 무수정** — `performanceData`가 `stamp` 키를 싣지 않으므로 설정은 `stamp.json`을 stamp.js가 직접 fetch(`window.showUrl`). 페이지 활성화도 stamp.js가 클래스 토글로 처리(숨긴 탭은 `activateTab`이 무시하기 때문). 다른 탭 클릭 시엔 기존 `activateTab`이 `#page-stamp`의 active를 정리.
- **상태는 자체 저장소** — 개발 중 기능이라 앱의 `buildStateSnapshot`/내보내기에는 아직 포함하지 않음(회귀 위험 최소화). 정식 승격 시 통합 검토.
- **선물 위치** — 이미지의 □ 위치는 행마다 달라(특히 7번은 2줄) 행별 좌표(`giftPos`)를 이미지에서 측정해 저장. 날짜는 선물 반대쪽에 배치.
- **더블데이/막칸** — 도장은 순차 배치라 더블데이 2개가 도장판 경계를 넘으면 자연히 다음 판으로 이어진다(“하나가 마지막이면 하나 더 만들어 찍기” 충족).
- **관극 판정** — 앱 공통 기준 `isEnded(p) && hasSeat(p)` 재사용. 자동은 sid로 중복 방지.
- **표시 순서** — 새로 만든 도장판이 위로 오도록 렌더는 역순(최신 먼저). 번호/이름은 생성 순서 인덱스를 유지(#2·#3 그대로).
- **자동 대상 선택 · 자동 채움 on/off** — 도장판이 여러 개일 때 자동이 채울 **기본 도장판(`autoTargetId`)** 을 도장판별 "자동 대상" 버튼으로 지정(다시 누르면 해제=최신 도장판 기본). 도장판별 **"자동 채움: 켬/끔"(`autoFill`)** 으로 자동에서 제외 가능. 대상 지정 시 자동 채움을 강제 on, 대상 도장판의 채움을 끄면 대상 지정 해제. **자동 대상이 꽉 차면 다른(자동 제외 포함) 도장판으로 넘기지 않고 새 도장판을 만들어 이어 찍는다**(새 판이 대상 승계). 지정이 없으면 자동채움 on이면서 안 찬 **최신** 도장판을 기본 대상으로. 배지(자동 대상/자동 제외)·툴바 정보에 현재 대상 표시. stamp.js ?v=3.
- **빈칸 클릭 = 공연 선택 팝오버** — 빈칸을 누르면 **안 찍은 회차(오래된 순)** 를 단추로 보여주고(가로 스크롤), 맨 왼쪽 **"임의로 넣기"**(SID 관리 안 함, 날짜+메모 자유 입력)를 sticky로 고정. 공연 단추를 누르면 그 회차(sid·날짜라벨·규칙 스탬프)를 그 칸에 찍는다. **SID로 연결된 칸**을 누르면 **지우기/바꾸기** 선택 → 바꾸기는 공연 선택 팝오버를 다시 연다(다른 회차/임의로 교체, 이전 sid는 다시 미도장으로). 임의 칸은 바로 임의 입력창(지우기 포함). 팝오버 바깥 클릭 리스너를 closePop에서 정리(팝오버 전환 시 조기 닫힘 버그 방지).
- **꽉 찬 도장판** — 자동 관련 메뉴(자동 대상·자동 채움 버튼/배지)를 숨긴다(대상이 될 수 없으므로). **닫힌(표지) 카드에는 찍힌 스탬프 개수(n/10)** 를 우상단에 표시.
- **색상** — 도장은 EXCELLENT·HOME COMING 모두 **붉은색**(테두리 포함, HOME COMING은 원형으로만 구분). 날짜는 **검은색에 가까운 볼펜색**(#1a1c22). stamp.js ?v=4.
- **공연 선택 표시 시점** — 공연 선택 팝오버는 **공연 시작 2시간 전부터** 그 회차를 보여준다(`isPickable`: 좌석 있음 + `now ≥ 시작−2h`). 자동/‘안 찍은 회차’ 집계는 기존대로 **종료된 관극**(`isEnded && hasSeat`) 기준 유지. 회차 접미사(-1/-2)는 종료 여부와 무관하게 **그 날 좌석이 있는 회차 수**로 계산해 라벨이 안정적. stamp.js ?v=5.
- **표지 이름 표시** — 캡션(이미지 아래 이름)을 없애고 표지 이미지 위에 표시. 제목이 **'Billy's Diary'** 그대로면 표기 없음(이미지에 이미 있음), **'Billy's Diary #숫자'** 형태(자동 #n이든 직접 그렇게 개명하든)면 'Billy's Diary' 옆에 **#숫자만**, 그 외 직접 지은 이름이면 스케치북 **오른쪽 아래**에 전체 이름. **찍힌 개수(n/10)** 는 스케치북 **오른쪽 위**. 카드 폭에 비례(컨테이너 쿼리 `cqi`).
- **표지 표현값은 stamp.json `cover`에서 설정(JS 하드코딩 금지)** — `cover.num`(위치 x/y·크기·기울기 rotate·색·italic)·`cover.name`(right/bottom·maxWidth·크기·기울기·색·italic)·`cover.baseTitle`(#n 패턴 기준). 숫자는 표지 스크립트 색(#a43233)·스크립트 기울기에 맞춰 기울임, 커스텀 이름은 **THE MUSICAL 색(#1b1b19)** 으로 흰 도화지 오른쪽 아래. 코드는 값을 인라인 스타일로 적용만 한다.
- **이름 변경 프리필** — 기본 이름(#n) 도장판은 rename 프롬프트를 **빈 칸**으로 열어, 그냥 확인만 눌러도 타이틀로 굳지 않는다(빈 값=기본 이름 유지).
- **다크모드 색 반전 방지** — 다이어리 카드(`.stamp-card`)에 `color-scheme:only light`. 밝은 테마가 `color-scheme:light dark`라 OS 다크모드가 고정 일러스트/도장 색을 반전(도장 핑크·날짜 흰색)시키던 것을 강제 라이트로 막는다. stamp.js ?v=8, styles.css v152.
- **이름 글꼴 · 번호 시작값** — 표지 이름/번호(`#n`) 오버레이 글꼴을 앱 본문 글꼴(**IBM Plex Sans KR**, `font-family:inherit`)로. 도장판 번호는 **첫 판부터 #1**. stamp.js ?v=10, styles.css v153.
- **명시적 이름 체계(기본 이름 유지 폐지)** — 이름을 동적으로 만들지 않고 **항상 저장**한다. 새 도장판은 `newBoard`가 **"기본제목 #n"** 을 명시 부여(`n` = 현재 `기본제목 #n`들의 **최대 숫자 +1**, `nextBoardNumber`). 옛 빈 이름 도장판은 로드 시 `migrateNames`로 명시 이름 부여. 이름 변경은 **빈 값 불가**(현재 이름 유지). 표시는 그대로 `기본제목 #숫자` 패턴이면 `#숫자`만, 아니면 커스텀 이름. stamp.js ?v=11.
- **커스텀 이름 표시 다듬기** — 크기 **지금의 80%**(`cover.name.size` 6→4.8), 위치 **왼쪽 0.3·아래 0.2 글꼴(em)** 이동(`cover.name.dx/dy`), **둥근 테두리 반투명 흰색 배경 칩**(도안 위에서도 글씨 구분). stamp.json?v=3, styles.css v154.
- **도장판 이미지 교체 · 선물 칸 제거 · 아코디언 펼침** — 새 도장판 이미지(945×1355, 선물 □ 없음)로 교체하고 그리드 재계산(`rowTop 0.1255`·`rowH 0.08155`·`checkX 0.6212`·`dayX 0.8503`, `giftRows:[]`). 선물 오버레이/토글 로직 제거. **표지-펼침 모델 변경**: 표지를 다른 화면으로 바꾸지 않고, 표지를 누르면 **그 아래에 도장판 표를 펼치고 다시 누르면 접는다**(아코디언, `renderCard`+`boardBodyHtml`, `.stamp-cover-box`/`.stamp-board-wrap`). 표지는 항상 표시. 메뉴(버튼 줄)를 **커버 위로** 올리고, 펼침 시 커버-도장판 사이 여백·그림자·맞닿는 모서리 라운드를 없애 **여백 없이 붙게**. stamp.json?v=6, stamp.js ?v=15, styles.css v157.
- **설정 로드 SHOW_BASE 대기(배포 슬러그 404 버그) · Tickets 탭 아이콘** — 배포 슬러그 페이지(`/billy/`)에서 `SHOW_BASE` 설정 전 `loadConfig`가 실행되면 상대경로 `stamp.json`이 **`/billy/stamp.json`으로 나가 404** → 설정 실패가 캐시되어 이미지가 안 뜨고 크림색만 나오던 버그. `baseReady()`로 `showUrl`이 절대경로(`/shows/<id>/…`)를 줄 때까지 기다렸다가 fetch하도록 수정. 더불어 **stamp.js의 JSON/이미지 접근을 app.js와 통일** — `dUrl(p)=dataUrl(showUrl(p))`(절대경로+배포 커밋 SHA `?v=BUILD`) + 로컬 `no-store`. 수동 `?v` 관리 불필요, 이미지는 `<img>`라 로컬만 `imgVer` 폴백. (기존: app.js=`dataUrl(showUrl)`+no-store, finale.js=`showUrl`+일부 수동 ?v, stamp.js=제각각 → stamp.js를 표준으로 정렬). **Tickets 탭**도 nav 압축 시 티켓 아이콘으로 축소(Stamp·Settings와 동일). stamp.js ?v=20, styles.css v162.
- **stamp.json도 콘텐츠 버전으로** — 커밋 SHA(BUILD) 버스트를 그만두고 `stamp.json?v=CFG_VER`(설정을 실제로 바꿀 때만 증가) + 로컬 `no-store`. 관계없는 커밋엔 재다운로드 안 함. (JS/CSS는 index.html의 `?v=NNN`으로 이미 콘텐츠 버전. 앱 전역 데이터 JSON(schedule/casts 등)은 여전히 app.js의 `dataUrl`=`?v=BUILD`라 매 배포 버스트 — 이는 앱 코어/워크플로 영역.) stamp.js ?v=23.
- **이미지 캐시 버스터를 콘텐츠 버전(imgVer)으로** — JSON/JS/CSS는 자주 바뀌어 커밋 SHA(`MAKULLIM_BUILD`)로 매 배포 버스트가 맞지만, **이미지는 거의 안 바뀌므로 SHA로 버스트하면 매 커밋마다 재다운로드되는 낭비**가 있었다. 이미지(`imgUrl`)만 `?v=CFG.imgVer`(콘텐츠 버전, 그림 교체 시에만 수동 증가)로 변경 → 관계없는 커밋에선 이미지 캐시 유지, 그림 실제 교체 + imgVer 증가 시에만 재다운로드. stamp.js ?v=22.
- **상시 Stamp 탭 · 이미지 캐시 버스터 · 도장판 이미지 재교체** — Finale와 Settings 사이에 **Stamp 탭** 추가(app.js `activateTab`에 `renderStamp` 훅 1줄, nav 넘칠 때 **도장 아이콘**으로 압축 — Settings 톱니와 같은 `nav.compact` 방식). 개발용 `#stamp`/`?stamp` 게이트도 유지(showPage가 `activateTab` 사용). **이미지 캐시 버스터**: 커버·도장판 `<img>`에 `?v=`(배포 커밋 SHA `MAKULLIM_BUILD`, 로컬은 `CFG.imgVer`)를 붙여, 파일명이 같아도 그림 교체 시 캐시가 갱신되게(`imgUrl`). 도장판 이미지 재교체(945×1355→1040×1490, `imgVer:2`). app.js ?v=208, stamp.js ?v=19, styles.css v161, stamp.json?v=9.
- **아이콘 UI·자동 대상 재설계·용어 변경** — 커버 텍스트 버튼을 **커버 스프링 위 동그란 아이콘**(오토=A+회전화살표·연필=이름변경·지우개=비우기·휴지통=삭제, `.stamp-icons`/`.stamp-ic`)으로. **오토(A) 아이콘 = 자동 채우기 대상**(하나만, 빈칸 있을 때만 선택, 꽉 차면 비활성, 다시 누르면 해제→자동 비활성). 상단 **자동 채우기** 버튼은 유효 대상 없으면 비활성. 자동 진행 중 대상이 꽉 차면: **팝오버**로 이어 찍을 곳을 고른다 — **새 재관카드** + 공간 남은 기존 재관카드 목록(**이름 (n/10)**) + 그만두기(`continueAutoFill`/`showContinuePopover`, 도장 단위로 재귀 이어붙임). 공간 남은 카드가 하나도 없으면 새 재관카드 자동 생성. `0/10` 개수는 **세로만 맨 아래로**(가로 유지). **다른 도장 찍기** 버튼 삭제, **새 도장판→새 재관카드**, **전체 지우기→모두 삭제**, **자동→자동 채우기**, 문구 전반 **도장판→재관카드**. `autoFill`(자동 제외) 개념 폐지. stamp.js ?v=17, styles.css v158.
- **#숫자 글꼴 Caveat** — 무료 손글씨 글꼴 **Caveat**(SIL OFL)를 번들(`fonts/Caveat-700.woff2` + `@font-face` + `fonts/Caveat-OFL.txt`). `cover.num.font`로 글꼴 지정(설정값), 코드는 인라인 `font-family`로 적용만. 손글씨라 별도 italic 없이(`num.italic:false`) rotate만으로 'Billy's Diary' 기울기에 맞춤. stamp.json?v=4, stamp.js ?v=12, styles.css v155.
- **순서 바꾸기(드래그 정렬)** — 재관카드를 원하는 순서로 재배열. **별도 '정렬 모드' 없음**(모드 진입·하단 '정렬 완료' 바 없음). **커버를 길게 누르면(≈480ms) 그 카드를 집어 곧바로 드래그**. **집은 카드는 `position:fixed`로 문서 흐름에서 빼 원래 크기·원색 그대로 포인터를 따라 떠다닌다** → 흐름에 남은 나머지 그리드가 흔들리지 않아 **삽입 위치 판정이 안정적(진동 없음)**. **원래 자리에는 점선 테두리 placeholder 상자(`stamp-ph`)를 남겨** 커버를 따라다니며 **놓일 위치로 이동해 미리보기**한다(어디에 들어갈지 보이게). 놓으면 커버가 placeholder 자리에 안착. **크기 축소는 '단일 열'일 때만**(`isSingleColumn()`로 판정 → `.stamp-shrink`) 적용한다: 단일 열(모바일)이면 나머지 카드·placeholder·**떠다니는 커버까지 모두 실제 폭 50%로 줄여**(`width:50%`, 높이·여백까지) 위·아래가 한눈에 보이게 하고, **다열(데스크탑 타일)이면 축소하지 않고 원래 크기 유지**(이미 여러 개가 보이므로). 나머지는 항상 회색으로 de-highlight, 드래그 중엔 본문 표·커버 아이콘 숨겨 커버만. (축소 시 떠다니는 커버의 `grabDX/DY` 오프셋도 스케일해 포인터 밑에 붙게 함.) **레이아웃(그리드/타일 배치)은 그대로 유지**해 순서가 계속 보인다. **위/아래(그리드면 좌우 포함)로 옮겨 놓으면 그 순간 순서 저장**(pointerup → `commitOrderFromDom`)·`position` 등 인라인 스타일과 클래스 제거돼 **즉시 원상 복귀**(본문 다시 펼침, `b.open` 유지). **화면 가장자리 근처면 스크롤 컨테이너(`.page`)도 함께 자동 스크롤**. 구현 요점: (1) **DOM 재생성 없이** 처리 → 누르고 있던 커버가 그대로 남아 길게 누르기→드래그 **끊김 없음**. (2) 재배치는 **포인터에 가장 가까운 카드**를 찾아 그 앞/뒤 삽입(`reorderByPointer`) — 같은 행에 다른 카드가 있으면 다열(그리드)로 보고 좌우, 아니면 단일 열로 보고 상하 판정 → 데스크탑 다열 타일·모바일 단일 열 모두 동작. 집은 카드가 `fixed`라 삽입해도 나머지 레이아웃에 영향이 없어 **같은 포인터 위치면 결과가 불변**(진동 방지). (3) **모바일 팝업 차단**: 커버가 `<img>`라 길게 누르면 브라우저 이미지 콜아웃/컨텍스트 메뉴가 떠 이동이 막히던 것을 `contextmenu` preventDefault + CSS `-webkit-touch-callout:none`·`user-select:none`·`draggable=false`로 차단. (4) **드래그 후 click(열기/닫기 토글) 억제**: pointerup 다음에 오는 click이 `b.open`을 토글해 닫힘/열림이 뒤집히던 버그를 `suppressClick` 플래그로 무시(숏탭 토글은 그대로). (5) 표시는 최신순이라 DOM 역순이 `st.boards`. stamp.js ?v=28, styles.css v169.
- **(버그수정) 모바일 롱프레스가 텍스트 선택으로 새 이동이 안 되던 문제** — 롱프레스 대기(≈480ms) 구간엔 커버에 `touch-action`이 안 걸려 있어 브라우저가 자체 롱프레스(선택/콜아웃, iOS ~500ms)를 발동, `pointercancel`로 드래그 타이머를 죽여 이동이 간헐적으로 실패했다. 수정: (1) `.stamp-card`에 **평상시부터** `user-select:none`·`-webkit-touch-callout:none`(전엔 `.stamp-dragging` 중에만) → 카드 어디를 눌러도 선택/콜아웃 시작 안 됨, (2) 누르는 동안만 `document`의 **`selectstart`를 preventDefault**(cleanup에서 해제)로 선택 제스처의 가로채기 차단(스크롤·포인터엔 무영향), (3) `LONGPRESS_MS` 480→**400**으로 낮춰 네이티브 롱프레스보다 먼저 드래그가 시작되게. stamp.js ?v=32, styles.css v172. **(후속)** iOS에서 여전히 선택이 되던 이유는 `touch-action`이 `auto`라 제스처 인식기가 롱프레스 선택을 그대로 실행했기 때문 — `.stamp-cover-box`·`.stamp-img`·커버 내부 오버레이(`touch-action` 비상속)에 **`touch-action:pan-y`** 를 걸어 **세로 스크롤은 유지하되** 인식기를 세로 팬으로 제한(선택/콜아웃 억제). styles.css v173. **(후속2)** 그래도 이미지를 길게 누르면 iOS가 선택을 **주변의 선택 가능한 페이지 텍스트(툴바·자동정보 "관극 N회…"·버튼)** 에 앵커해 전체 선택이 됐다(카드에만 `user-select:none`이라 카드 밖 텍스트가 앵커 대상). → **`#page-stamp` 전체에 `user-select:none`·`-webkit-touch-callout:none`** 적용(스탬프 페이지엔 복사할 텍스트가 없어 안전). styles.css v174. **(후속3)** 커버 **왼쪽/중앙(=맨 이미지 영역)** 을 누르면 롱프레스가 금방 취소되고 오른쪽(아이콘·오버레이)만 되던 문제 — `<img>`를 직접 누르면 iOS가 이미지 드래그/콜아웃 인식기를 돌려 `pointercancel`을 쏴 타이머가 죽었다(`draggable=false`·`user-drag:none`으로도 발생). → `.stamp-img`·오버레이 텍스트에 **`pointer-events:none`** 을 걸어 터치를 뒤의 `.stamp-cover-box`(div)로 넘겨 이미지 인식기 우회(아이콘 버튼은 클릭 유지). styles.css v175.
